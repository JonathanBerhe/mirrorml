name: rounding_precision_timestamp_001
bucket: synthetic
category: rounding_precision
description: Offline timestamp resolution 'ms'; online resolution 'us'.
expected_divergences:
- category: rounding_precision
expected_localization:
- op_kind: source
  side: both
offline:
  language: sql
  source: offline.sql
  schemas:
    events:
    - - ts
      - timestamp[ms, UTC]
online:
  language: sql
  source: online.sql
  schemas:
    events:
    - - ts
      - timestamp[us, UTC]
generator:
  module: bench.scripts.generate_synthetic
  version: 1
