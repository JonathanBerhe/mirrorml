name: rounding_precision_decimal_001
bucket: synthetic
category: rounding_precision
description: Offline decimal(10, 0); online decimal(10, 2).
expected_divergences:
- category: rounding_precision
offline:
  language: sql
  source: offline.sql
  schemas:
    transactions:
    - - amount
      - decimal[10, 0]
online:
  language: sql
  source: online.sql
  schemas:
    transactions:
    - - amount
      - decimal[10, 2]
generator:
  module: bench.scripts.generate_synthetic
  version: 1
