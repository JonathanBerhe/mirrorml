"""MirrorBench tooling: pair loader, synthetic generator, evaluator.

This package is internal to the benchmark; it is not part of the
``mirrorml`` library's public API and is not installed when ``mirrorml``
is. The scripts here run directly from the repo via ``python -m
bench.scripts.<name>``.
"""

from __future__ import annotations
